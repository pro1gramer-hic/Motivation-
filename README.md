# Motivation-
No need description 
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Force et Persévérance - Votre Devise</title>
    
    <style>
        /* --- Styles Généraux et Mise en Page --- */
        body {
            margin: 0;
            padding: 0;
            font-family: 'Georgia', serif; 
            
            /* Fond dégradé sombre et élégant */
            background: linear-gradient(135deg, #101c3e 0%, #1e3c72 100%); 
            color: #f0f0f0; 
            
            /* Centrage parfait au milieu de l'écran */
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            text-align: center;
        }

        .container {
            background-color: rgba(0, 0, 0, 0.4); 
            padding: 50px 70px;
            border-radius: 10px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.7); 
            max-width: 650px;
            width: 90%;
            border: 1px solid rgba(255, 255, 255, 0.1); 
        }

        /* --- Style du Proverbe --- */
        .proverbe {
            font-size: 1.6em;
            font-weight: 300;
            line-height: 1.8;
            margin: 0 0 40px 0;
            padding-left: 20px;
            border-left: 4px solid #d4af37; /* Ligne dorée */
            text-align: left;
        }

        .proverbe p {
            margin: 10px 0;
        }

        /* Accentuation de la conclusion */
        .signature {
            font-weight: bold;
            font-style: italic;
            color: #d4af37; /* Couleur dorée */
            margin-top: 30px !important;
            text-align: right;
            font-size: 1.05em;
        }

        /* --- Style du Bouton --- */
        .bouton-action {
            display: inline-block;
            padding: 12px 25px;
            background-color: #d4af37;
            color: #101c3e; 
            text-decoration: none;
            border-radius: 5px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: background-color 0.3s, transform 0.3s;
        }

        .bouton-action:hover {
            background-color: #c0a02c; 
            transform: translateY(-2px); 
        }
    </style>
    </head>
<body>

    <div class="container">
        <blockquote class="proverbe">
            <p>Si tu es mis à l’épreuve, c’est que tu peux la réussir ;</p>
            <p>l’échec n’a de force que la vulnérabilité...</p>
            <p class="signature">... et le succès n'a d'allié que la persévérance.</p>
        </blockquote>

        <a href="#" class="bouton-action">Commencer l'épreuve</a>
    </div>

</body>
</html>
